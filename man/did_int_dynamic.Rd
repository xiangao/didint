% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did_int_dynamic.R
\name{did_int_dynamic}
\alias{did_int_dynamic}
\title{Dynamic DR DATT with interference (event study, common adoption)}
\usage{
did_int_dynamic(
  data,
  yname_pre,
  ynames,
  treat,
  exposure,
  g,
  covariates,
  event_time = NULL,
  coords = NULL,
  cutoff = NULL,
  dist_fn = c("spherical", "euclidean"),
  trim = NULL,
  alpha = 0.05,
  aggregate = TRUE
)
}
\arguments{
\item{data}{A data frame in wide format. Must contain a single
pre-period outcome column and one column per post-period outcome.}

\item{yname_pre}{Character. Column name of the pre-period outcome
(period \code{c - 1}).}

\item{ynames}{Character vector of column names for the post-period
outcomes, ordered chronologically (period \verb{c, c+1, ..., T}).}

\item{treat, exposure, g, covariates}{See \code{\link[=did_int_2x2]{did_int_2x2()}}.}

\item{event_time}{Optional integer vector matching \code{ynames}, giving
event time relative to treatment (e.g. \code{0:3} for c, c+1, c+2, c+3).
Used to label the per-period results. Defaults to \code{seq_along(ynames) - 1}.}

\item{coords, cutoff, dist_fn, trim, alpha}{See \code{\link[=did_int_2x2]{did_int_2x2()}}.}

\item{aggregate}{Logical. If \code{TRUE} (default), also returns a simple
average across post-periods.}
}
\value{
A list of class \code{"didint_dynamic"} with:
\describe{
\item{per_period}{Data frame: one row per post-period with
\code{event_time}, \code{estimate}, \code{se}, \code{ci_lo}, \code{ci_hi}.}
\item{agg}{If \code{aggregate = TRUE}, a list with \code{simple_avg}
(mean of per-period estimates) and \code{se} (SE of the average,
accounting for shared influence functions across periods).}
\item{models}{Per-period model objects (returned by \code{did_int_2x2}).}
}
}
\description{
Implements Section I of Xu (2026). With common adoption timing \code{c},
computes the doubly robust DATT at exposure level \code{g} for each
post-treatment period, taking the long difference \verb{Y_t - Y_\{c-1\}}
as the outcome of interest.
}
\details{
Because adoption timing is common, exposure does not drift across
post-periods: the 2x2 DR estimator is applied period-by-period with
the same treatment indicator and same exposure variable. The same
parallel-trends assumption must hold for \emph{each} post-period
separately (Assumption 1 of Xu 2026 applied to every \code{t >= c}).
}
\examples{
# Common adoption, 4 post-periods. Same DGP as did_int_2x2() example
# but with multiple post outcomes.
set.seed(2)
N <- 800; T_post <- 4
lon <- runif(N, 0, 10); lat <- runif(N, 0, 10)
z   <- 0.3 * lon + 0.2 * lat + rnorm(N)
W <- rbinom(N, 1, plogis(-0.5 + 0.6 * z))
dij <- as.matrix(dist(cbind(lon, lat)))
A <- (dij < 1.5) & (dij > 0)
share <- (A \%*\% W) / pmax(rowSums(A), 1)
G <- as.integer(share > median(share))
Y_pre <- 0.8 * z + rnorm(N)
df <- data.frame(W = W, G = G, z = z, Y_pre = Y_pre,
                 lon = lon, lat = lat)
for (k in 1:T_post)
  df[[paste0("Y_post_", k)]] <- Y_pre + 0.2 * k * z + 1.5 * W +
                                 0.5 * G * W + rnorm(N)

res <- did_int_dynamic(df, yname_pre = "Y_pre",
                       ynames = paste0("Y_post_", 1:T_post),
                       treat = "W", exposure = "G", g = 1,
                       covariates = "z", trim = 0.01)
res$per_period      # per-period direct ATTs
res$agg$simple_avg  # cross-period average; truth is 2.0

}
\seealso{
\code{\link[=did_int_2x2]{did_int_2x2()}} for the 2x2 building block.
}
